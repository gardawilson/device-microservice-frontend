name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy with Docker Compose
        run: |
          cd /opt/device-service-frontend
          git pull origin main
          docker compose up -d --build
          docker image prune -f
